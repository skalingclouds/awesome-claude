name: Content Validation

on:
  pull_request:
    paths:
      - "apps/web/**"
      - "packages/**"
      - "emails/**"
      - "content/**"
      - "scripts/**"
      - "examples/content/**"
      - "integrations/raycast/**"
      - "tests/**"
      - ".trunk/**"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/workflows/**"
      - "cloudflare/api-schema-heyclaude-openapi.yaml"
      - "README.md"
      - "CHANGELOG.md"
      - "renovate.json"
      - "package.json"
      - "pnpm-lock.yaml"
      - "playwright.config.ts"
      - "vitest.config.ts"
  workflow_dispatch:
  schedule:
    - cron: "17 9 * * 1"

permissions:
  contents: read
  checks: write
  deployments: read

jobs:
  validate-content:
    runs-on: ubuntu-latest
    env:
      TRUNK_API_TOKEN: ${{ secrets.TRUNK_API_TOKEN || secrets.TRUNK_TOKEN }}
      TRUNK_ORG_URL_SLUG: ${{ secrets.TRUNK_ORG_URL_SLUG || secrets.TRUNK_ORG_SLUG }}
    concurrency:
      group: content-validation-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0

      - name: Reject direct PR README changes
        if: ${{ github.event_name == 'pull_request' }}
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          if [ "$HEAD_REPO" = "$GITHUB_REPOSITORY" ]; then
            case "$HEAD_REF" in
              automation/submission-*|automation/readme-refresh)
                exit 0
                ;;
            esac
          fi

          if git diff --name-only "$BASE_SHA...HEAD" -- README.md | grep -q '^README.md$'; then
            echo "::error file=README.md::README.md is generated by maintainer automation. Remove README.md from this PR; CI regenerates it for validation and the post-merge README Refresh PR owns the committed update."
            exit 1
          fi

      - name: Setup pnpm
        uses: pnpm/action-setup@4f4305e1fe60ad818b8ae6fc4a697cc47eab0b2d

      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f
        with:
          node-version: 24.15.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Clean test reports
        run: rm -rf reports/junit && mkdir -p reports/junit

      - name: Build registry artifacts
        run: pnpm --filter web run prebuild

      - name: Regenerate README for pull request validation
        if: ${{ github.event_name == 'pull_request' }}
        run: pnpm generate:readme

      - name: Validate cleanup policy
        run: pnpm validate:clean

      - name: Apply local D1 migrations
        run: pnpm --filter web db:migrate:local

      - name: Validate local D1 jobs schema
        run: pnpm validate:d1-jobs -- --local

      - name: Validate task tracker
        run: pnpm validate:tasks

      - name: Run Vitest suite
        run: pnpm test

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Run browser regression suite
        run: pnpm test:e2e

      - name: Install Raycast dependencies
        working-directory: integrations/raycast
        run: npm ci

      - name: Test Raycast helpers
        working-directory: integrations/raycast
        run: npm run test:junit

      - name: Install Trunk CLI
        run: |
          curl https://get.trunk.io -fsSL | bash -s -- -y
          echo "$HOME/.trunk/bin" >> "$GITHUB_PATH"

      - name: Upload test results to Trunk
        if: ${{ always() && env.TRUNK_API_TOKEN != '' && env.TRUNK_ORG_URL_SLUG != '' }}
        continue-on-error: true
        run: trunk flakytests upload --junit-paths "reports/junit/*.xml" --org-url-slug "$TRUNK_ORG_URL_SLUG" --token "$TRUNK_API_TOKEN"

      - name: Validate content schema
        run: pnpm validate:content:strict

      - name: Validate category submission spec
        run: pnpm validate:category-spec

      - name: Validate issue templates
        run: pnpm validate:issue-templates

      - name: Validate generated OpenAPI schema
        run: pnpm validate:openapi

      - name: Validate downloadable packages
        run: pnpm validate:packages

      - name: Validate Raycast feed
        run: pnpm validate:raycast-feed

      - name: Validate email templates
        run: pnpm validate:emails

      - name: Dry-run Resend template sync
        run: pnpm resend:sync-templates -- --dry-run

      - name: Verify generated registry artifacts are current
        run: git diff --exit-code apps/web/public/data apps/web/src/generated

      - name: Audit content report
        run: pnpm audit:content

      - name: Verify README is up to date
        run: pnpm validate:readme

      - name: Type-check web app
        run: pnpm type-check

      - name: Build web app
        run: pnpm build

      - name: Lint Raycast extension
        working-directory: integrations/raycast
        run: npm run lint

      - name: Build Raycast extension
        working-directory: integrations/raycast
        run: npm run build

      - name: Trunk Check
        run: trunk check --ci --all

  validate-pr-preview:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_DEV_WORKER_URL: ${{ vars.CLOUDFLARE_DEV_WORKER_URL || 'https://heyclaude-dev.zeronode.workers.dev' }}
    concurrency:
      group: deployment-artifacts-pr-preview-${{ github.repository }}
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@4f4305e1fe60ad818b8ae6fc4a697cc47eab0b2d

      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f
        with:
          node-version: 24.15.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Deploy same-repo PR preview to dev Worker
        id: deploy-preview
        if: ${{ github.event.pull_request.head.repo.full_name == github.repository && env.CLOUDFLARE_API_TOKEN != '' }}
        run: |
          pnpm --filter web run deploy:dev
          echo "base-url=$CLOUDFLARE_DEV_WORKER_URL" >> "$GITHUB_OUTPUT"

      - name: Resolve PR preview URL
        id: preview-url
        env:
          GITHUB_TOKEN: ${{ github.token }}
          PREVIEW_DEPLOYMENT_URL: ${{ steps.deploy-preview.outputs.base-url || env.CLOUDFLARE_DEV_WORKER_URL }}
        run: node scripts/resolve-pr-preview-url.mjs --allow-missing

      - name: Validate deployed artifact contract
        if: ${{ steps.preview-url.outputs.base-url != '' }}
        run: pnpm validate:deployment-artifacts -- --base-url "${{ steps.preview-url.outputs.base-url }}"

      - name: Validate deployed MCP endpoint contract
        if: ${{ steps.preview-url.outputs.base-url != '' }}
        env:
          MCP_ENDPOINT_URL: ${{ steps.preview-url.outputs.base-url }}/api/mcp
        run: pnpm validate:mcp-endpoint
