node_modules
.next
.open-next
.wrangler
.DS_Store
dist
coverage
reports
test-results
# `reports` (above) is for build/test output dirs; keep the source API route dir tracked.
!/apps/web/src/routes/api/reports/
.build-content-index.lock/
TASKS.md
.claude/
.internal/
.dev.vars
.dev.vars.*
.env.local
.env.*.local
docs/jobs-listings-private-flow.md
integrations/raycast/package.json.dev-backup

# Ensure first-party package assets remain tracked even if a global ignore has *.zip
!content/skills/*.zip
!content/mcp/*.mcpb

# Build-time generated website projections.
apps/web/public/data/
apps/web/public/downloads/
apps/web/src/generated/
apps/web/src/routeTree.gen.ts

# Incremental type-check cache (tsc --noEmit .tsbuildinfo; restored from CI cache).
apps/web/.cache/

# D1 schema migrations must stay tracked even if a global ignore has *.sql
!apps/web/migrations/
!apps/web/migrations/*.sql
!apps/submission-gate/migrations/
!apps/submission-gate/migrations/*.sql
*.mdx
